import React from 'react';

import LoginFormContainer from './LoginFormContainer';

const Login = (props) => {

    return (
        <div>
            <h1>Login</h1>
            <LoginFormContainer />
        </div>
    )
}

export default Login;